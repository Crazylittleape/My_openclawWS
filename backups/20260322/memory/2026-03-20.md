# 2026-03-20

- `love-personality-mvp` 新增了“完整报告下载 PDF”链路：前端完整报告页加入 `下载 PDF 报告` 按钮，后端新增 `POST /api/report/export-pdf`，使用 `reportlab` 生成正式排版 PDF。
- PDF 样式已做一轮修正：用户反馈有陌生符号/菱形乱码与颜色偏浅；已在 `backend/app/services/pdf_report_service.py` 中移除易乱码字符（如中间点分隔）、避免富文本标签干扰，并把标题/正文/边框颜色整体调深。
- 用户对前端文案偏好非常明确：不接受 AI 味、宣传味或生硬措辞，尤其不喜欢“拆你的xx”这类表达；要求对整个前端界面再做一轮自检和全局润色，优先更自然、更像人写的话。
- 已对 `prototype/index.html`、`prototype/app.js`、`prototype/data.js` 做一轮批量文案清理，重点替换了“拆/画像/人生说明书/底层/高维/贴脸/有点准”等高 AI 感词，并确认 `app.js` 与 `data.js` 语法检查通过。
- `love-personality-mvp` 当天继续完成 P0 修复：后端补了对旧 `single` 模块语义的兼容（统一映射到 `unbound_single`），完整兑换→选模块→开始测试→提交→看报告→导出 PDF 链路已实跑通过，新旧卡密路径都正常。
- 后端正式报告已从 scaffold/dummy 方案切成“前端真实计算、后端真实落库”的结构：前端提交 `result_type` 与 `dimension_snapshot`，后端生成正式报告内容；实测报告标题为真实结果而非占位文案，PDF 已确认嵌入 WenQuanYi 中文字体并能规避特殊符号乱码。
- 生产安全配置做了一轮落地：后台管理员旧密码失效，新强密码已生效；登录限流验证通过（连续错误登录会触发 429）；`backend/app/core/config.py`、`app/main.py`、`app/api/admin_auth.py`、`app/services/auth_service.py` 已加入更明确的 production 配置、会话/Host/CORS 参数化和安全头。
- 用户当天目标是把 `love-personality-mvp` 推到“可上市版本”；因此又对移动端首页做了一轮上线向修复，重点是移动端信息减法、压低模块卡片高度、隐藏重复说明区，并把“输入激活码”提升为手机端首屏主 CTA，减少已购用户迷路概率。
